#!/bin/bash

echo ""

/home/martin/bin/i3subscribe mode | while read -r event; do
  mode=$(echo $event | cut -c 6-)

  if [ "$mode" == "default" ]; then
    echo ""
  else 
    echo "$mode"
  fi

done

